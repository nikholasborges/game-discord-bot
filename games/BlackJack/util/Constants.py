game_status_list = []

current_game = None


# TODO: implement logic to check if the guild already have a game running

def check_game_status(guild_id, game):
    if len(game_status_list) > 0:
        for game_status in game_status_list:
            pass


def add_game_status(guild_id, game):
    pass


def remove_game_status(guild_id, game):
    pass
